node_modules
dist
.nuxt
.output
.data
.env
.env.*
!.env.example
.wrangler
playground/.nuxt
playground/.output
playground/.data
playground/.wrangler
playground/playwright-report
playground/test-results
playground/blob-report
playground/.playwright
coverage
*.log
*.tgz
.DS_Store

# Better Auth scaffolds these into the module's root when `dev:prepare`
# loads the module (the real files live in playground/).
.nuxtrc
auth.config.ts
server/auth.config.ts

# Local tooling state (not part of the package)
.opencode/goals/
.vscode/settings.json

# Stray root-level solutions dir from older CE setup; learnings live in docs/solutions/.
/solutions/
cookies.txt
