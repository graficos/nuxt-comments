# Publish settings.
# `access` only applies to scoped packages; explicit here so the intent is
# clear if the package is ever renamed under a scope.
access=public

# Provenance is enabled by publishing from CI with OIDC (e.g. GitHub Actions):
#   provenance=true
